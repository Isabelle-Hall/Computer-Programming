# When a file is being processed the file object maintains a value that refers to the current position within the file.
# We can read the position using the tell() method, and change it using the seek() method.
# Using seek() allows a program to perform random access. (allows indexing)